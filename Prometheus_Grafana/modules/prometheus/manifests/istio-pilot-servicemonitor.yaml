apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: istio-pilot
  namespace: ${namespace}
  labels:
    app: istio-pilot
spec:
  selector:
    matchLabels:
      app: istiod
  namespaceSelector:
    matchNames:
      - istio-system
  endpoints:
  - port: http-monitoring
    interval: 15s
