# Terraform变量配置示例
# 复制此文件为 terraform.tfvars 并根据实际情况修改
# 大部分变量使用模块默认值，只需定义环境特定值

# Kubernetes节点名称（必填，请通过 kubectl get nodes 查看）
node_name = "kubernetes-default-pool-xxxxxxxx-xxxxx"

# 查看节点命令：
# kubectl get nodes -o wide

# MinIO管理员凭证（生产环境请使用强密码）
minio_root_user = "admin"
minio_root_password = "MinIO@Admin2024@Secure"

# MinIO副本数量（单节点部署建议设为1）
minio_replicas = 1

# 资源限制（根据实际情况调整）
minio_cpu_request = "500m"
minio_cpu_limit = "2000m"
minio_memory_request = "1Gi"
minio_memory_limit = "4Gi"

# 服务类型
service_type = "LoadBalancer"
